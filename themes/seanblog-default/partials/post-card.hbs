<a class="sb-card" href="{{url}}">
  {{#if coverImage}}<div class="sb-card-cover"><img src="{{coverImage}}" alt=""></div>{{/if}}
  <div class="sb-card-body">
    <h3 class="sb-card-title">{{title}}</h3>
    {{#if excerpt}}<p class="sb-card-excerpt">{{excerpt}}</p>{{/if}}
    <div class="sb-card-meta">
      {{#if publishedAt}}<time>{{format_date publishedAt format="YYYY-MM-DD"}}</time>{{/if}}
      {{#if category}}<span>{{category.name}}</span>{{/if}}
      {{#if commentCount}}<span>{{commentCount}} 评论</span>{{/if}}
    </div>
  </div>
</a>
